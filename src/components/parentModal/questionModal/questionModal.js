import React from "react";
import ParentModal from "../parentModal";

const QuestionModal = ({ setModal, modal }) => {
  return (
    <ParentModal setModal={setModal} modal={modal}>
      <div>
        
      </div>
    </ParentModal>
  );
};

export default QuestionModal;
